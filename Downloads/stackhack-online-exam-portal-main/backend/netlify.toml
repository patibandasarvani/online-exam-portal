[build]
  command = "npm run start"
  publish = "."

# Redirect all to functions or server (placeholder - Netlify isn't ideal for Node servers without functions)
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/server"
  status = 200
